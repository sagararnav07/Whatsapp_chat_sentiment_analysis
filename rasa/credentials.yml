# Rasa Analyser — Credentials
# =============================
# Channel credentials for Rasa.
# The REST channel is used for direct HTTP API access.

rest:
  # No authentication needed for local development.
  # For production, add token-based auth.

# Uncomment below to enable other channels:
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true
